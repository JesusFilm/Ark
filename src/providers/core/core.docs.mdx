---
description: "A Collection of all elements components."
labels: ["react", "typescript", "core"]
---

import { JesusFilmThemeProvider } from ".";

## React Component for Core

A component that references all other provider components. This simplifies the number of components external consumers need to import.

```js live
<JesusFilmThemeProvider />
```

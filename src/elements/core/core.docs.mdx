---
description: "A Collection of all elements components."
labels: ["react", "typescript", "core"]
---

import { Donate } from ".";

## React Component for Core

A component that references all other element components. This simplifies the number of components external consumers need to import.

```js live
<Donate />
```
